# ============================================================================
# SWIFT-LLM Environment Configuration
# ============================================================================
# Rename this file to .env and fill in your API keys
# mv env.example.txt .env
# ============================================================================

# ----------------------------------------------------------------------------
# API Keys (at least one required for inference)
# ----------------------------------------------------------------------------

# Groq API Key (Recommended - Fast inference)
# Get your free key at: https://console.groq.com/
GROQ_API_KEY=gsk_your_groq_api_key_here

# OpenAI API Key (Optional - For premium tier)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# ----------------------------------------------------------------------------
# Cache Configuration
# ----------------------------------------------------------------------------

# Similarity threshold for semantic cache hits (0.0 - 1.0)
# Lower = more cache hits, Higher = stricter matching
# Default: 0.70 (recommended for balanced precision/recall)
SWIFT_CACHE_THRESHOLD=0.70

# Default model tier when classification fails (1-5)
# 1=Cache, 2=Fast, 3=Balanced, 4=Quality, 5=Premium
SWIFT_DEFAULT_TIER=2

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
SWIFT_LOG_LEVEL=INFO

